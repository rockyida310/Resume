```
mongodb stores data in bson format which is binary representation of json (javascript object notation)

query results a cursor which allows traversal over the result set of the query

upsert option type - allows to update a document if exists or delete an document if not when upsert params is true

```